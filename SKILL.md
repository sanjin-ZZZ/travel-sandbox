---
name: travel-sandbox
description: Use when a user wants a personal interactive travel sandbox or a repeatable travel-planning website with cohesive illustrated city maps, itinerary, food, lodging, and travel ledger, including a phone home-screen delivery flow.
metadata:
  short-description: 标准旅行沙盘：了解偏好、搜集灵感、讨论行程、生成同款网页并交付手机入口
---

# 标准旅行沙盘

默认复用米白纸色、深绿界面、四个功能入口与紧凑布局。地图采用有地形起伏和体积感的完整插画：地形、建筑、植被与地名统一绘制；“3D画风”指插画透视，不默认使用真实3D引擎。用户给出其他明确风格时遵从其选择。内容密度、主次和片区随用户需求调整，不复制演示旅行。

现有 `assets/template.html` 是旧版 SVG 功能底座，生成命令只能产出功能初稿，不能宣称已完成最终插画样式。制作最终页面必须读 [视觉与实现规范](references/visual-system.md)，使用随包的 `assets/illustrated-reference/` 完成插画层和统一排版，再验收。该参考代码是可复用实现材料，不含任何个人旅行图片、路线或账目。

## 开始前

- 从当前对话和用户材料提取已确定信息；已有行程就跳过重复问卷。目的地没定时先做目的地比较，不拿演示数据替用户决定。
- 问清年份、出发/返程日期及已订交通、人数、预算口径、兴趣/不喜欢、体力与节奏、住宿要求、主要设备。只补问真正缺失的信息，一次少量；允许“没想好”。问一个“过去让你舒服的旅行片段”帮助理解偏好，不强制深度采访。
- 锁定硬约束，预算区分每人/总额/每晚，航班区分落地/起飞。后续不得悄悄改日期、城市或预算。
- 需要联网检索能力、可用的浏览器或来源接口，以及本地 Node.js 18+。缺哪项直接说清，不伪造检索、试用或部署结果。本 Skill 不要求用户安装查看器或原生 App。

## 工作流

1. **轻量画像。** 简短复述确定项和待定项，再开始搜集。
2. **收集灵感。** 用户只说了少量景点，不等于只想看到这几个点。根据兴趣与路径补充有用的候选景点、酒店、餐饮和休息点；用户明确只要所列地点时遵从其范围。备选数量随需求调整，不固定补十个，也不为填满画面加入不感兴趣的地点。 优先用户指定的平台；小红书、游记、本地社区用于体验线索，官方/地图/预订平台用于核实。中途不要求用户逐条批准搜索。来源不可访问时说明，询问是否接受其他来源；不声称“看过”无法访问的帖子。整理主选/替换备选、适合原因、来源及待核实项。不得擅自点赞、收藏到账号、发帖或联系商家。
3. **大纲讨论。** 给出按天的大致节奏和少量有意义的选项，说明差异，不先写满分钟级安排。用户确认方向后，再讨论影响选择的细节。已有完整确认行程时直接进入下一步。
4. **串联核实。** 核对地理关系、交通估时、营业/预约、住宿和机场缓冲；时长标注估计与来源。餐饮备选不自动塞入饭点；未选备选不计入路线；天气替换不与主选同时安排。活动必须匹配实际旅行年份和日期；价格必须说明日期、人数/房型及核实状态。参考 [数据与填写规则](references/data.md)。
5. **生成标准沙盘。** 先读 [需求适配与地图层次](references/adaptive-map.md)，再读 [视觉与实现规范](references/visual-system.md)，以 `assets/example-trip.json` 的结构填写新 `trip.json`，复用功能底座并完成插画实现。每城通常一张独立固定沙盘，主安排较大、次要候选较小；根据真实地点分布调整排布，放大后可探索小店与小景点。日期选择对应城市，不能只换右侧日程却留着另一城地图；同城坐标不随日期变化。全部已收集地点进入图或待定位索引，不默默截断数量。
6. **验收与交付。** 按 [产品验收](references/standard.md) 检查实际页面。最后按 [部署与手机](references/delivery.md) 交付。按当前 Agent 的可用能力选择通用 HTTPS 托管，不绑定 GPT 或 Sites；明确手机访问是否需要登录，遵从已授权的访问范围，公开前须有本次旅行的公开授权。当地酒店、票务与交通由用户自行预订。

## 生成命令

`<skill>` 指本 SKILL.md 所在目录，使用真实路径替换。

```sh
node "<skill>/scripts/generate.mjs" "/absolute/path/trip.json" "/absolute/path/new-site"
```

不需要安装 npm 依赖。输出目录必须不存在：先生成 `index.html`、`manifest.webmanifest`、`icon.svg`、`apple-touch-icon.png`。最终版本还须包含引用的城市插画等资源，部署完整构建目录，不能只上传这四个文件。数据校验失败时修正 JSON 中对应字段，不删校验、不猜缺失值、不以旧数据冒充新行程。更新使用新构建目录，旅行 `id` 保持不变；新旅行使用新 `id`。

## 固定边界

- 四入口：每天怎么玩、想吃什么、住与行、旅行记账。记账、收藏、完成标记、住宿已订标记、备注和备份都是标准功能，不随意删掉。
- 地图是一张统一设计的插画，不把独立矢量模型和字体硬贴到另一画风的背景上。插画不冒充建筑实测图；示意坐标不能当经纬度。未核实的店址进入待定位索引。
- 同城切日期不换底图；点地点只更新详情与选中提示，不自动放大。路线沿插画中的道路走，选中用柔光和小路牌，不用虚线框。四页共用字号、侧栏和顶栏，删去口号、装饰英文和重复大标题。
- 保留人民币单币种账本：收支、退款、付款人、人数人均、总预算和分类统计。不自动换汇、不读取支付账户、不把酒店候选价记成实际支出。需要其他币种/AA时先说明这超出现有版本，不暗中修改模板。
- 记录按旅行编号隔离，仅存当前浏览器；不承诺跨设备同步。导入前校验同一旅行编号并确认替换。原旧版个人旅行备份不自动兼容本标准版。
- 手机使用独立紧凑界面，默认摘要一两屏、详情点开、地图整幅适配；不能把电脑长页面直接缩小。
- 主屏幕入口不等于离线安装。当前标准版需要联网打开，未实现 service worker 缓存；不要为用户承诺离线重开。
- 不使用原作者的账户、域名、托管项目、私人资料或本地路径。分享 Skill 不等于获得发布其他用户行程的授权。

## 模板维护

`assets/src/` 与 `assets/template.html` 保存功能底座；`assets/illustrated-reference/` 提供最终样式的地图交互与布局实现。普通旅行需要重新制作城市插画、热点和沿路轨迹，不能仅换 JSON 就宣称复现画风。先在新输出工程中完成适配，保持旅行 ID、地点 ID 与存储键不变。修改模板源码后运行 `npm run rebuild`、`npm test`；依赖缺失时先安装。验收实际页面后才交付，不能以编译成功代替视觉通过。
